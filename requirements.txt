asgiref==3.11.1
Django==6.0.4
mysqlclient==2.2.8
python-dotenv==1.2.2
sqlparse==0.5.5

# --- AI agents (Branch A) ---
# Both agents (Concierge + Data Architect) use Google Gemini. Optional at
# runtime: the app falls back to an offline client if the key/SDK is absent.
google-generativeai==0.8.3

# --- A3.4: real flight/hotel data via RapidAPI ---
requests==2.34.2
