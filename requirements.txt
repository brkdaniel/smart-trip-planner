asgiref==3.11.1
Django==6.0.4
mysqlclient==2.2.8
python-dotenv==1.2.2
sqlparse==0.5.5

# --- AI agents (Branch A) ---
# Concierge agent uses Anthropic Claude; Data Architect (Week 2) uses Gemini.
# Optional at runtime: the app falls back to an offline client if these are
# absent or no API key is set.
anthropic==0.40.0
google-generativeai==0.8.3
